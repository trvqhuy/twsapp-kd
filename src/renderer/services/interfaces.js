/**
 * MarketDataService
 * - getLastPrice(symbol)
 * - subscribe(symbols, callback)
 * - unsubscribe(symbols)
 */

/**
 * PortfolioService
 * - getAccountSummary()
 * - getPositions()
 * - getOrders()
 */

/**
 * ExecutionService
 * - submitOrder(orderRequest)
 * - cancelOrder(orderId)
 * - onEvent(callback)
 */

export const SERVICE_DOC = "Interfaces only";
